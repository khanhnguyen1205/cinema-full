# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A **React 18 + TypeScript** cinema booking single-page app (student project, FER202 course), built with **Vite 6**, on a **real backend**: a modular **Express 5 + TypeScript** server (`server/src/`, port 4000) talking to **PostgreSQL via Prisma**. That one server does three jobs: **auth** (bcrypt passwords + JWT access/refresh tokens in **httpOnly cookies**), an **authorization gateway** at `/api/*` (per-collection/role rules, then Prisma queries), and — in production only — **serving the built SPA from the same origin**. There is no mock API: **json-server was removed in GĐ3c**; `db.json` survives purely as the **seed source** for `server/prisma/seed.ts`.

The codebase is **TypeScript-first**: source is `.ts`/`.tsx`. `allowJs` is on, so three files remain `.jsx` and interop untyped — `src/routes/PrivateRoute.jsx`, `src/routes/AdminRoute.jsx`, `src/components/admin/Modal.jsx` (a re-export shim). All server code is `.ts`.

## Commands

```bash
npm install            # install deps (postinstall runs `prisma generate`)
npm run dev            # start both servers at once (auth/API + web) via concurrently
# — or run them separately:
npm run auth           # Express auth + API gateway on :4000 (tsx, no watch)
npm start              # Vite dev server on :3000

npm run build          # production build: vite build (→ build/) + tsc (→ server/dist/)
npm run build:server   # server only
npm run start:prod     # node server/dist/index.js (needs NODE_ENV=production to serve the SPA)
npm run preview        # serve the Vite build on :3000

# database (Prisma → Postgres)
npm run prisma:migrate # migrate dev (creates + applies a migration)
npm run prisma:deploy  # migrate deploy (production/CI, idempotent)
npm run prisma:seed    # load db.json into the database (wipes + reinserts, keeps ids)
npm run prisma:studio  # browse the data

# quality gates (also run in CI)
npm run typecheck      # tsc --noEmit (app) && tsc -p server/tsconfig.json (server)
npm run lint           # ESLint 9 (flat config); CI requires 0 warnings
npm run lint:fix
npm run format         # Prettier write
npm run format:check   # Prettier check (CI)
npm run test           # Vitest (watch)
npm run test:run       # Vitest once (no coverage)
npm run test:cov       # Vitest once + coverage; this is the CI gate (thresholds enforced)
npm run e2e            # Playwright (chromium): smoke + full booking flow
```

**Six CI gates** (`.github/workflows/ci.yml`) must stay green: `typecheck` · `lint` (0 warnings) · `format:check` · **`test:cov`** (503 Vitest unit tests **+ coverage thresholds**) · `e2e` (Playwright) · `build`. A seventh job, **`docker`**, builds the image so the Dockerfile is exercised on every push (the dev machine has no Docker). The `e2e` job spins up a **Postgres 16 service container**, runs `prisma migrate deploy` + seed, then Playwright.

### Ports & running the app
Two servers in dev: Express **auth + API** on **:4000**, Vite dev server on **:3000** (open http://localhost:3000). Client service files point at **:4000** through **one** module, `src/services/http.ts`, which exports `API_URL` (`:4000/api`) and `AUTH_URL` (`:4000`) for `api.ts`/`auth.ts`/`payments.ts`/`email.ts`. URLs come from `.env` (`VITE_API_URL`, `VITE_AUTH_URL`) with fallbacks; copy `.env.example` → `.env`. **The fallback is branched on `import.meta.env.PROD`** — `"/api"` and `""` (same origin) in a production build, the `:4000` host only in dev. That branch is load-bearing, not cosmetic: the Docker build has **no `.env`** (`.dockerignore` excludes it), so an unconditional `localhost:4000` fallback gets baked into the shipped bundle and every visitor's browser calls back to *their own* machine, which CORS blocks — the deployed SPA then hangs on the splash screen forever while `curl` against the API keeps looking perfectly healthy. This shipped undetected from GĐ3f until 2026-07-28 because the live site had only ever been verified with `curl`, never opened in a browser. The server needs **`DATABASE_URL`** (+ `DIRECT_URL` for migrations) or it refuses to start. A `SessionStart` hook (`.claude/start-dev.ps1`) auto-starts both when a session begins.

In **production** there is only **one** service on one port: the same Express app serves `/api` + `/auth` **and** the SPA build (`server/src/static.ts`), so cookies stay same-origin and CORS is switched off.

**Gotchas:**
- The server runs under `tsx` with **no watch**, so **edits to `server/**` need a manual restart** (kill the listener on :4000, `npm run auth`) — in-memory seat holds reset with it. Vite HMR (src/) hot-reloads on its own.
- **Renaming a file's extension** (`.jsx`→`.tsx`) can white-screen Vite HMR. Fix: kill :3000, `rm -rf node_modules/.vite`, `npm start`.
- On Windows a running server **locks the generated Prisma client**, so `npm install` / `prisma generate` fail with EPERM. Kill :4000 first.
- Don't import `server/src/env.ts` from anything with a unit test: it **throws when `DATABASE_URL` is missing**, and the CI `checks` job has no database (that is why `static.ts` reads `process.env.NODE_ENV` directly).

## Architecture

Data flows: **React pages → TanStack Query hooks (`src/queries/*`) → `src/services/*` fetch helpers → Express gateway (:4000) → Prisma → Postgres**. Server state lives in **TanStack Query** (v5); the only global client state is `AuthContext`. `fetch` wrappers are thin and all sent with `credentials:"include"` so the session cookie reaches the gateway.

### Project structure & imports
`src/` is organized by type with lowercase folder names: `components/` (shared UI; `components/ui/` = the design-system primitives, `components/admin/` = admin-only widgets), `routes/` (route guards `PrivateRoute`/`AdminRoute`), `context/` (`AuthContext`), `hooks/` (`usePagination`), `lib/` (pure helpers — `pricing.ts`, `seatNav.ts`, `cx.ts`, `reviewStats.ts`), `queries/` (TanStack Query hooks + key registry), `services/` (`api.ts`/`auth.ts`), `types/` (`index.ts` domain models), `pages/` (one page + colocated `.css`; `pages/booking/` = the wizard, `pages/admin/` = the admin panel, `pages/dev/` = DEV-only), and `styles/` (`tokens.css` / `utilities.css` / `global.css`). `App.tsx` and `index.tsx` stay at `src/` root (Vite entry points).

**Absolute imports** are enabled via `tsconfig.json` paths (`baseUrl: "src"`) + a matching alias in `vite.config.mjs`. Import cross-module code from the `src`-root path — `import Navbar from "components/Navbar"`, `import { useAuth } from "context/AuthContext"`, `import { useMovies } from "queries/catalog"`, `import "styles/global.css"` — **not** relative `../../`. Same-folder siblings still use relative paths (`./Modal`, a page's own `./Home.css`). **When adding a new top-level `src/` folder, register it in BOTH `tsconfig.json` paths and `vite.config.mjs` resolve.alias.**

- **`server/prisma/schema.prisma`** — the data model, 9 models: `User`, `Movie` (has `rating`, a number ~6.8–9.0 shown as the star score everywhere — this is a **critic-style score**, kept separate from audience reviews), `City`, `Cinema` (→ `City`), `Room` (→ `Cinema`; `type` 2D/3D/IMAX + seat-layout `rows`/`cols`/`vipRows`, plus `coupleRows` and `aisleAfterCols`), `Showtime` (→ `Movie`, → `Room`; `price` = standard-seat price, `bookedSeats` = pre-sold seat numbers), `Booking` (+ `paymentRef`, the Stripe PaymentIntent id — `@unique`, null for counter payments), `Concession` (F&B catalog), `Review` (audience rating). **There is no `seats` table** — seats are generated from the room layout and booked seats are derived (see Booking flow). Two deliberate fidelity decisions from the json-server era: `time`/`createdAt` are **`String`** (not `DateTime`) so serialization matches what the client always saw, and **`Booking` has no foreign keys** — it is a snapshot, so deleting a movie/room never cascades into someone's ticket. FK integrity exists only along the catalog chain City→Cinema→Room→Showtime→Movie. **`Review` is the exception that DOES take an FK** (→ `Movie`, `onDelete: Cascade`) — a review only means something while its movie exists — plus `@@unique([movieId, userId])` (one review per user per movie, `P2002`→409) and a `verified` flag stamped once at create-time (true if the caller has a booking for that movie → the "Đã xem" badge). `src/types/index.ts` mirrors all of these client-side.

- **`db.json`** — **no longer a database**, only the seed fixture read by `server/prisma/seed.ts` (wipes each table, reinserts **keeping ids**, then resets the Postgres sequences and asserts row counts match). Passwords in it are already bcrypt hashes. **Its dates are fixed in the file but shifted at seed time** by `server/src/schedule/date-shift.ts` (pure, tested): one offset is applied to every showtime `time`, booking `createdAt` and review `createdAt`, chosen so the fixture's earliest day lands on `today − PAST_DAYS` (2). The 7-day window therefore always straddles now — 2 days past, today, 4 ahead — which keeps relative spacing and times-of-day intact while guaranteeing bookable showtimes. Seeded with the raw dates instead, the whole catalogue silently rots: every showtime falls into the past, "Sắp tới" is permanently empty and Home advertises films you cannot book. Re-seed after a long gap and it re-centres itself. Re-seeding is no longer the only cure: the server re-centres the window itself on boot and every 6h (see `schedule/*`), so a live deployment stops going empty a few days after a seed — but that mechanism only *shifts* existing rows, it never creates them, so the first seed is still manual.

- **`src/lib/pricing.ts`** — pure helpers: `buildSeatLayout(room)` (rows×cols → seat grid with `isVip`/`isCouple`; couple rows hold half the units + aisle handling), `bookedSeatSet(showtime, bookings)` (union of `showtime.bookedSeats` and matching bookings' seats), `vipPrice(base)` = `round(base×1.3)` and `couplePrice(base)` = `round(base×1.6)` to nearest 1,000, `priceOf(seat, base)`, `seatType(seat)`, F&B helpers `fnbLines`/`fnbTotal`, and constants `ROOM_TYPE_PRICE`, `SERVICE_FEE`, `MAX_SEATS` (8), `MAX_ITEM_QTY` (10). `src/lib/seatNav.ts` — `nextSeat(layout, current, dir)` powers the seat map's keyboard roving-tabindex. `src/lib/search.ts` — `normalize` (strips Vietnamese diacritics + đ→d + lowercase), `matches`, `scoreMatch`; the shared search core behind `GlobalSearch`, the `/search` page, and Movies' name filter (so "endgame"/"dien bien" match accent-insensitively). `src/lib/time.ts` — `nowKey()` + `isUpcoming(time, now?)`, the single rule for "this screening hasn't started": DB times are naive local strings, so "now" must be formatted the same way — comparing against `new Date().toISOString()` (UTC) skews by the timezone offset and hides the next 7 hours of showtimes in VN. `MovieDetail`, `CinemaDetail`, `/search` and `GlobalSearch` all filter through it, so a past screening is never offered for booking. All have colocated `*.test.ts`.

- **`src/services/http.ts`** — the **only** definition of the backend's base URL: `API_URL` + `AUTH_URL`, each `import.meta.env.VITE_*` with the `import.meta.env.PROD` fallback branch described under "Ports & running the app". Every service (`api`/`auth`/`payments`/`email`) imports from here. That expression used to be copy-pasted four times — and it is exactly the code whose dev-only fallback hung the live site on its splash screen for four days. **Never inline it again**; a fifth copy is a fifth chance to get the prod branch wrong.

- **`src/services/api.ts`** — all movie/showtime/city/cinema/room/booking calls, hitting the gateway (`API_URL`) with `credentials:"include"`. `getShowtimesByCinema(cinemaId)` fetches the cinema's rooms then gathers showtimes per room. `getOccupiedSeats(showtimeId)` hits the gateway's computed `/api/occupied-seats` (only seat numbers, no personal data — merges booked seats **and** seats other users are actively holding) — the booking flow uses this instead of `getBookings()` for availability, because the gateway scopes `GET /bookings` to the caller. `holdSeats(showtimeId, seats)` / `releaseSeats(showtimeId)` drive the server-side seat hold. `create*/update*/delete*` helpers back admin CRUD (movies/rooms/showtimes + booking update/delete — **`cinemas` are read-only**). **Note:** `getAllShowtimes` fetches every showtime then filters client-side. There is **no** `getSeats`/`updateSeat`.

- **`src/queries/`** — the **TanStack Query** layer (v5). `keys.ts` is the single source of truth for cache keys (the `qk` registry). `catalog.ts` = read hooks (`useMovies`/`useCinemas`/`useCities`/`useAllShowtimes`/`useRooms`/`useMovie`/`useCinema`/`useShowtimesByMovie`/`useShowtimesByCinema`). `booking.ts` = `useOccupiedSeats(id,{poll})` (poll → `refetchInterval:10s`), `useConcessions`, `useMyBookings`, `useCreateBooking`. `reviews.ts` = `useMovieReviews(movieId)` + `useCreateReview`/`useUpdateReview`/`useDeleteReview` (each `invalidateQueries` the movie's reviews + `allReviews`). `admin.ts` = `useAllBookings` + `useAllReviews` + create/update/delete mutations for movie/room/showtime/booking, each `invalidateQueries` on success (so there is **no manual refetch**). The `QueryClientProvider` (staleTime 60s, retry 1, no refetch-on-focus) wraps the app inside `ErrorBoundary`, outside `AuthProvider`; `ReactQueryDevtools` mount in DEV only. Imperative seat hold/release stay as plain calls (side effects tied to selection + cleanup), not mutations.

- **`server/src/`** — the backend, one module per job. `index.ts` (listen) → `app.ts` (middleware + mount order) → `auth/*` · `api/*` · `db/prisma.ts` · `static.ts` · `env.ts`.
  - **`auth/routes.ts`** — `POST /auth/register|login|logout|refresh`, `GET /auth/me`. Passwords are **bcrypt-hashed**; sessions are **JWT access (15m) + refresh (7d)** in **httpOnly, SameSite=Lax cookies** (JS can't read them ⇒ XSS-safe), `secure` only in production. Login is rate-limited (`skipSuccessfulRequests` → only failed attempts count, 10/IP/15m). Emails are normalized (trim+lowercase). `remember` → persistent refresh cookie (Max-Age 7d) vs a session cookie. Legacy plaintext passwords self-upgrade to bcrypt on first successful login. `JWT_SECRET` has a dev fallback that **throws** under `NODE_ENV=production`. Supporting files: `tokens.ts` (sign/verify), `cookies.ts`, `middleware.ts` (`getUserFromReq`), `helpers.ts` (+ tests), `users.ts` (the only place that touches the `User` table).
  - **`api/gateway.ts`** — the catch-all `/api/*` authorization layer: **catalog** (`movies/showtimes/cinemas/cities/rooms/concessions`) is public read, admin-only write; **`users`** is admin-only; **`bookings`** GET is scoped to the caller (admin sees all), POST forces `userId` to the caller, PATCH/DELETE are admin-only; **`reviews`** GET is public (filter by `?movieId=`), POST needs login and stamps `userId`/`userName`/`verified`/`createdAt` server-side (validating rating 1–5 + comment ≤500 via `reviews-validate.ts`), PATCH/DELETE are **owner-or-admin** (`ownerOrAdmin`). It holds **no data logic** — every allowed request is handed to `handleRest`. Unlike the other branches, the reviews branch **imports the prisma singleton** (to compute `verified`, look up `fullName`, and check ownership) — that is fine because `gateway.ts` has no unit test.
  - **`api/repo.ts`** — `handleRest(req, res, rest, extraFilters?)` translates REST to Prisma while preserving the **exact HTTP contract the client was written against**: POST → **201**, DELETE → **`{}` + 200**, unknown id → **404**, lists ordered **`{ id: "asc" }`** (drop this and the movie order on Home scrambles), Prisma `P2025` → 404, `P2003`/`P2002` → 409. Bodies are filtered through a per-collection whitelist so `id` and stray fields can't be written. It **returns the row it wrote** (POST/PATCH; `undefined` elsewhere) purely so the caller can do follow-up work — the gateway needs the new booking's id to send the ticket email; every other call site ignores the value.
  - **`api/collections.ts`** — pure metadata backing `repo.ts` (which fields are filterable and their type, which are writable, which are Json) plus `parseFilters`/`pickWritable`. **Imports nothing from Prisma on purpose** so its unit tests run in CI without a database.
  - **`api/occupied.ts`** — `GET /api/occupied-seats?showtimeId=` returns only seat numbers (no personal data): `showtime.bookedSeats` ∪ seats from that showtime's bookings ∪ seats **other** users are holding.
  - **`api/holds.ts`** — **the one piece of server state**: an in-memory `Map<showtimeId, Map<seat, {userId, expiresAt}>>`, 8-minute TTL (matches the client `SeatHoldTimer`), never persisted. `POST /api/holds {showtimeId, seats}` replaces + renews the caller's holds (also the heartbeat; **409** with `conflicts` if someone else holds a seat), `DELETE /api/holds?showtimeId=` releases, and a successful `POST /bookings` clears them. This is why the deployment must stay **single-instance**.
  - **`schedule/*`** — keeps the seeded 7-day showtime window from rotting. `date-shift.ts` is **pure** (no Prisma/env, so its unit tests run in CI without a database) and is the single source for all date math: `server/prisma/seed.ts` imports it too — the import only works in that direction, because `server/tsconfig.build.json` sets `rootDir: server/src` while `seed.ts` runs under `tsx` and is never compiled. `planShift(times, today)` returns the number of days to shift, or **`null` meaning leave it alone** — null when there are no showtimes, when the latest one is still `AHEAD_DAYS_MIN` (2) calendar days out, or when the computed offset is 0. `refresh.ts` reads every showtime, asks `planShift`, and rewrites `Showtime.time` in one `$transaction`; it **never throws** (a slow database must not take the server down) and a module-level `running` flag stops the boot run and the interval from overlapping. It is started from **`index.ts`, not `app.ts`** — `app.ts` is imported by the supertest unit tests, so mounting the timer there would fire it on every test run. **Only `Showtime.time` is touched**: `Booking.createdAt`/`Review.createdAt` are real timestamps of real people, so seeded bookings drift into looking bought-after-the-screening, which is a cosmetic wart on fixture data and deliberately not fixed.
  - **`static.ts`** — production only: `express.static("build")` + an SPA fallback for every path that isn't `/api` or `/auth`. **Mounted last** in `app.ts`; mounting it earlier would make API calls silently return `index.html`. Files under `/assets` are served `Cache-Control: immutable, max-age=1y` because their names carry a content hash; `index.html`, `sw.js` and the manifest deliberately stay uncached, or users get stuck on an old build.
  - **`payments/*`** — Stripe test-mode. `quote.ts` + `verify.ts` are **pure** (no Prisma/env/stripe imports, so their unit tests run in CI without a database): `quote()` recomputes an order's amount from base price + seat rows + F&B lines — a **deliberate copy** of `src/lib/pricing.ts`'s rules, since `server/tsconfig.build.json` sets `rootDir: server/src` and cross-imports are impossible; both sides are locked by hard-coded number tests. `checkIntent()` accepts a PaymentIntent only when `status==="succeeded"` + amount matches + currency `vnd` + `metadata.userId` is the caller. `stripe.ts` lazily builds the client and reads `process.env` directly; `isStripeEnabled()` is false when either key is missing, which **turns the card method off instead of breaking the app**. `amount.ts` is the single place that tra DB → `quote()` (also rejecting seats already sold or held by others with a **409 + `conflicts`** *before* charging). `settle.ts` verifies one card payment for the gateway. `routes.ts` serves `GET /api/payments/config` (public: `{enabled, publishableKey}` — never the secret key) and `POST /api/payments/intent` (login required; **server computes the amount, never trusting the client's number**).
  - **`email/*`** — ticket email via **Resend**, called with plain `fetch` (Node 22 has it; the API is one JSON POST, so **no SDK dependency**). `templates.ts` + `lang.ts` are **pure** (no Prisma/env/resend imports, so their unit tests run in CI without a database): `renderTicketEmail(data, lang)` builds subject/HTML/plain-text in vi or en from its own small string table (server can't import `src/i18n`, same `rootDir` constraint as `quote.ts`), `splitTime()` **slices the ISO string instead of going through `new Date()`** — the DB string carries no timezone, and Render runs UTC while the browser is UTC+7, so a `Date` round-trip would print an hour different from the ticket on screen — and `formatVnd()` groups digits itself rather than depending on Node's ICU build. `qr.ts` renders the QR PNG (same payload as `ETicket.tsx`) which is sent as an **attachment** `ve-TK-000xx.png`; Gmail blocks `data:` images, so an attachment is the only thing that renders everywhere. `send.ts` loads the booking + user/movie/showtime/cinema/room and **never throws** — `gateway.ts` fires it with `void` *after* the booking response has already gone out, so a mail failure can never cost a paid ticket. `routes.ts` serves `GET /api/emails/config` (public `{enabled}`) and `POST /api/emails/ticket` (login + **owner-or-admin**, reusing `ownerOrAdmin` from `reviews-validate.ts`, rate-limited 5/15m) — the resend path, which *is* synchronous and reports the real provider error. `isEmailEnabled()` is false when `RESEND_API_KEY` is missing, which **hides the resend button instead of breaking the app**, exactly like the Stripe card method.
  - **Mount order in `app.ts` is load-bearing**: `/auth` → `/api/occupied-seats` → `/api/holds` → `/api/payments` → `/api/emails` → `/api` (catch-all) → SPA. Express matches in order, and Express 5 rejects `app.all("/api/*")` (use `app.use`).

- **`src/services/auth.ts`** — client wrappers calling the auth server with `credentials:"include"` (cookies): `loginUser(email,password,remember)`, `registerUser(...)`, `logoutUser()`, `refreshSession()`, `fetchMe()` (retries once via `/auth/refresh` on a 401). No token is ever exposed to JS. Error messages are in Vietnamese.

- **`src/context/AuthContext.tsx`** — the only global client state. Wraps the app in `App.tsx`; **no localStorage** — on mount it hydrates the user via `fetchMe()` (cookie session) behind a `loading` gate (`AppShell` in `App.tsx` shows a splash until the check resolves). Exposes `useAuth()` → `{ user, loading, login, logout }`. Premium session UX: instant **cross-tab sync** via `BroadcastChannel("cinema-auth")`, **silent refresh** every 13m, and **idle auto-logout** after 30m. When a refresh fails the user is cleared → route guards redirect to `/login`.

- **`src/routes/PrivateRoute.jsx`** — gates routes; redirects to `/login` (preserving intended location in `state.from`) when `user` is null.

- **`src/routes/AdminRoute.jsx`** — role gate for `/admin`: redirects to `/login` if logged out, to `/` if `user.role !== "admin"`. Users carry a `role` field (`"user"` | `"admin"`); the seeded admin is `admin@cinema.vn` / `admin123`, a normal user is `a@cinema.vn` / `123456`.

### Deployment (Docker, single service)
`Dockerfile` is multi-stage: stage 1 (`node:22`) runs `npm ci` (whose `postinstall` does `prisma generate` — no database needed) then `npm run build`, producing `build/` + `server/dist/`; stage 2 (`node:22-slim`, `openssl` + `ca-certificates` for the Prisma engine and TLS to Postgres) installs prod deps only and copies the two build outputs. Start command: `prisma migrate deploy && node server/dist/index.js` — idempotent, so restarts are safe. `PORT` comes from the platform; `app.set("trust proxy", 1)` in production so the rate limiter sees real client IPs behind the proxy. `.env` is excluded via `.dockerignore` — secrets come from the platform's env vars (`DATABASE_URL`, `DIRECT_URL`, `JWT_SECRET`, `NODE_ENV=production`).

The dev machine has no Docker, so the image is only ever built by **CI** (`docker` job) and the host. What *is* verifiable locally is prod-mode without Docker: `npm run build`, then `NODE_ENV=production PORT=4100 JWT_SECRET=… npm run start:prod` — one port serves both the SPA and `/api`.

### Design system — "Kinetic" (neo-brutalist cinematic)
The UI is a **custom design system in plain CSS — no UI library**. Design tokens (CSS variables: colors, type scale, spacing, radius/border, motion) live in **`src/styles/tokens.css`**; `utilities.css` holds a couple of helpers; `global.css` is the reset + `@import`s of the two. The look is **neo-brutalist "Kinetic"**: a warm near-black ground (`--bg` #0d0c0b, deliberately *not* a cold absolute black), hard 1–2px borders (`--r-sm`/`--bw-*`), and inverted **"bone" blocks** (`--surface-invert` cream + `--text-invert` black) for emphasis, all respecting `prefers-reduced-motion`. Fonts are self-hosted via `@fontsource` (Anton / Barlow / Barlow Condensed / IBM Plex Mono) — no CDN; **every face must ship a `vietnamese` subset** (see `src/styles/fonts.ts`).

**Two colour rules and four type roles carry the identity — don't dissolve them back.** The palette has *two* accents with disjoint jobs: **red is action and warning only** (đặt vé, ghế đã bán, mục đang mở) and **`--amber` #f0a830 is time and score** (giờ chiếu, dải suất chiếu, sao đánh giá, điểm phim). Red used to be identity *and* button *and* decorative numeral at once, which is how it stopped signalling anything. Typography splits four ways — `--font-display` (Anton, uppercase) is **only** film titles, the logo and large money figures; `--font-head` (Barlow Condensed **500**, sentence case, `letter-spacing: 0`) is every page/section/dialog heading; `--font-body` is prose and controls; `--font-mono` is **only** machine data (giờ, mã ghế, mã vé, giá). One display face at every level meant the page shouted at every level. **The type scale is deliberately restrained** (`--fs-xl` section headings max 38px, `--fs-2xl` hero film title max 68px, page titles max 46px): the first pass shipped them at 56/120/88px, which is just another way of shouting. `KineticHeading` is a *poster* effect — it forces `--font-display` + uppercase, so it belongs on film/cinema names only, never on a list page's own title. **The brand logo must stay ≥24px**: its red "MA" is `--red` #e63030 at 4.49:1, legal only as WCAG large text — shrinking it to `--fs-lg` reddened the a11y gate on every page at once.

**`N°` survives in exactly two places** — the hero carousel dots and the four booking steps — because those are the only real sequences. `Section` therefore has **no `index` prop**; section eyebrows carry information instead (`7 phim còn suất`, `5 rạp tại 3 thành phố`). The home page has **no marquee, no vanity stats band and no closing CTA**: the marquee became `.tonight-k`, a user-scrolled strip of the next 12 real showtimes that links straight to seat selection, and it is the one element allowed to be loud.

Reusable primitives live in **`src/components/ui/`** (barrel `index.ts`): `Button`, `Tag`, `Badge`, `Card`, `Rule`, `Field`, `Skeleton`, `Spinner`, `IconButton`, `Numbered` (only its `formatIndex` helper is used, by the two real sequences), `KineticHeading`, `Marquee` (no production consumer left — kept as a primitive, demoed in the kitchen sink), `Reveal`, `TicketEdge`, `Modal`, `Container`, `Section`, `Grid`, `StarRating` (keyboard-navigable input + readonly display, amber stars) — shared styles in `ui.css`. Page-level kinetic classes are suffixed `-k` (e.g. `nav-k`, `hero-k`, `movie-k`, `adm-k__table`). A DEV-only route **`/kitchen-sink`** (`pages/dev/`, mounts only when `import.meta.env.DEV`) previews every primitive.

### Routing (`src/App.tsx`)
`/` Home · `/movies` Movies · `/movie/:id` MovieDetail · `/cinemas` Cinemas · `/cinema/:id` CinemaDetail · `/search` Search (unified: movies + cinemas + upcoming showtimes, reads `?q=`) · `/login` · `/register` · `/seats/:showtimeId` (protected) · `/tickets` (protected). Pages live in `src/pages/`, each with a colocated `.css` file. `BrowserRouter` opts into the React Router v7 future flags.

**The booking wizard and the whole admin area are `React.lazy`** behind one `<Suspense fallback={<RouteFallback/>}>`. That is not premature optimization: `recharts` (~200KB, admin-only) sat in the main bundle, and **`@stripe/stripe-js` injects the `js.stripe.com` script the moment it is imported**, so a static import put a third-party cookie and an extra network round-trip on every public page. Splitting them cut unused JS on the home page from 694 KiB to 156 KiB and LCP from 10.6s to 5.5s — see `docs/superpowers/plans/2026-08-01-lighthouse-baseline.md`. Keep new heavy, rarely-visited routes lazy too.

The global search box lives in the Navbar (`components/GlobalSearch`): a debounced combobox with a grouped movie/cinema/showtime dropdown, keyboard-navigable (↑↓/Enter/Esc), Enter → `/search?q=`. Movies filters (name/genres/rating/duration/room-format/city/date/sort) are **URL-synced via `useSearchParams`** — shareable, bookmarkable, back/forward works; `/search`'s "Phim" section links to `/movies?q=…` to carry the query into the detailed filters. All search is **client-side** over the already-cached TanStack Query data — no gateway search endpoint. **Don't wrap unbounded result grids in `Reveal`** (its IntersectionObserver `threshold: 0.15` never fires for a section taller than viewport/0.15 — ~5.6k px on mobile — hiding it permanently).

Admin routes are nested under `/admin` (wrapped in `AdminRoute` → `AdminLayout` with a sidebar + `<Outlet>`): index `AdminOverview` (stats + recharts revenue charts), `movies` `AdminMovies`, `rooms` `AdminRooms`, `showtimes` `AdminShowtimes`, `bookings` `AdminBookings`, `reviews` `AdminReviews` (moderation: list every review, search + star-filter, delete) — all in `src/pages/admin/` sharing `Admin.css`. Data + CRUD go through the `queries/admin.ts` mutation hooks (movies/rooms/showtimes + booking edit/cancel + review delete — **`cinemas` are read-only**). Shared `src/components/admin/Modal.jsx` (re-exports `ui/Modal`) + `ConfirmDialog.tsx` + `Pagination.tsx` back the tables/forms, and **the six tables share two more modules — use them instead of re-deriving the same table**: `src/pages/admin/AdminUI.tsx` (presentational: `AdminHead`, `SearchBox`, `CinemaFilter`, `RowActions`, `ModalActions`, `TablePager` — the last one exists because `usePagination` returns `setPage` while `Pagination` wants `onPage`) and `src/pages/admin/adminUtils.ts` (stateful: `adminDateTime`, `useById`, **`useAdminForm`** — every field stays a **string** to match `<input>`, the page converts with `Number()` on save, and its optional `derive` callback fills dependent fields, today only "pick a room → suggest the price for that room type" — and **`useConfirmDelete`**, which is where a page's guard lives: skipping the API call still closes the dialog). Deleting a movie or room is **guarded client-side**: blocked (alert) while any showtime still references it. `AdminBookings` can **edit a booking's seats** (mini seat map) and **cancel** a booking (both admin-only via the gateway).

### Internationalization (i18n)
The app is **bilingual (Vietnamese default + English)** via **`react-i18next`**, wired in **`src/i18n/`**: `index.ts` inits i18next (`fallbackLng:"vi"`, `interpolation.escapeValue:false`, and — on `languageChanged` — stamps `document.documentElement.lang`); it is imported once in `src/index.tsx`. **Language detection is `localStorage`-only** (order `["localStorage","htmlTag"]`, key **`lang`**) — deliberately **no `navigator`**, so a fresh Playwright/CI context always renders **vi** and the write specs' Vietnamese assertions stay stable. Translations live in `locales/vi.json` + `locales/en.json`, keyed by area namespace (`common`, `nav`, `footer`, `genres`, `home`, `movies`, `search`, `movieDetail`, `cinemas`, `booking`, `tickets`, `auth`, `admin`) — **add every new key to both files**. `format.ts` exports the formatters everything routes through: **`formatPrice`** (always VND — only the digit grouping is locale-aware), **`formatDateTime`/`formatDate`** (locale-aware, read `i18n.language` at call time so a module-level `const fmt = …` still re-localizes per render), plus three **named presets that used to be hand-copied across six files** — reach for these before writing a fresh option bag: **`formatDayShort`** (the date-strip label on Movies/MovieDetail/CinemaDetail), **`formatClock`** (a showtime's hour — deliberately pinned to `en-GB` 24h, *not* locale-aware, because `en-US` renders "7:30 PM", which breaks the showtime-button grid and disagrees with the hour printed on the ticket), and **`formatShowtimeLabel`** (hour + weekday/day/month, for search results in `GlobalSearch` + `/search`). `genres.ts` `genreLabel(code)` maps a **DB genre code** (`Action`/`Sci-Fi`/…) to its translated label with a fallback to the code — **DB content itself is never translated**, only such fixed enums. `components/LanguageSwitcher` (VI/EN buttons, in the Navbar desktop + mobile) calls `i18n.changeLanguage`. When adding UI copy, pull it through `t("area.key")` — **including aria-labels** (the `ui/` primitives `Modal`/`Skeleton`/`Spinner`/`StarRating` do).

### PWA
Installable + offline-capable via **`vite-plugin-pwa`** (Workbox `generateSW`), configured in **`vite.config.mjs`**. `registerType:"prompt"` — a new deploy shows a **reload toast** (`components/PWAUpdatePrompt`, using `virtual:pwa-register/react`, mounted once in `App.tsx`) rather than reloading mid-session. The **manifest** (name/short_name/icons 192·512·512-maskable/`display:standalone`/`theme_color:#0a0a0a`) is injected into the built `index.html`. Workbox **precaches the shell** (`globPatterns` includes `woff2` so self-hosted fonts work offline) with `navigateFallback:"index.html"` + `navigateFallbackDenylist:[/^\/api/,/^\/auth/]`. **Runtime caching is whitelist-only:** a single `StaleWhileRevalidate` rule matches **public catalog GETs** (`/api/{movies,showtimes,cinemas,cities,rooms,concessions,reviews}`) — it deliberately **never caches** `/api/bookings`, `/api/users`, `/api/occupied-seats`, `/api/holds`, or `/auth/*` (personal data + seat-holds must stay live). **`devOptions.enabled` is false**, so the SW is **off under `npm run dev`** — Playwright e2e (which runs on the dev server) never sees a stale-cache SW. A custom **install button** (`components/InstallButton`, hidden on mobile top bar ≤720px — lives in the mobile menu there) captures `beforeinstallprompt` via the `hooks/useInstallPrompt` store (module-level `useSyncExternalStore` so multiple instances stay in sync) and only renders when installable; strings are in the `pwa` i18n namespace. **Icons** are generated once by `scripts/gen-icons.mjs` (headless Chrome via Playwright, embedding the Bebas Neue woff2) into `public/*.png` + `public/favicon.svg` (a red "C" on near-black). In prod, `static.ts` serves `sw.js`/`manifest.webmanifest`/icons from `build/` at the root (backend unchanged). **Verify PWA via prod-mode build**, not e2e (SW is off in dev): `npm run build` then check `build/sw.js` + `build/manifest.webmanifest`.

### Booking flow
Users reach a showtime two ways: from a movie (`MovieDetail`: city → cinema → date → showtime) or from a cinema (`Cinemas` → `CinemaDetail`). The `/seats/:showtimeId` route renders **`BookingWizard`** (in `src/pages/booking/`), a 4-step flow: ① seats → ② concessions (F&B) → ③ payment → ④ e-ticket (real QR, the reusable `components/ETicket`). It builds the seat grid from the room layout (`buildSeatLayout`), marks taken seats from `useOccupiedSeats`, and drives the **server-side seat hold**: it calls `holdSeats` whenever the selection changes (also a heartbeat across steps), **polls `getOccupiedSeats` every 10s** (via `refetchInterval`) in the seat step to reflect others' holds, handles the **409** conflict by dropping the clashing seats, and `releaseSeats` on leaving the page / hold expiry. The seat map is keyboard-navigable (roving tabindex, `lib/seatNav.ts`). A shared `SeatHoldTimer` (8 min) mirrors the server TTL. On confirm it re-checks availability then `POST`s **one** booking (`{ cinemaId, roomId, seats, seatTypes:{standard,vip,couple}, concessions, totalPrice, … }`) via `useCreateBooking` — no per-seat PATCH, so no partial-write problem. `MyTickets` reads the caller's bookings (`useMyBookings`, gateway-scoped), enriches each with movie/showtime/cinema/room, reuses `ETicket`, and splits into upcoming/past by showtime date.

**Step ③ has two payment methods, built dynamically from `usePaymentConfig()`.** "Tại quầy" (pay at the counter) always exists and creates the booking straight away, exactly as before. "Thẻ quốc tế (Stripe)" appears **only when the server has both Stripe keys** — otherwise it is hidden and nothing else changes, so the app never breaks on a missing env var. The card path uses Stripe's **Payment Element in deferred mode** (`{mode:"payment", amount, currency:"vnd"}` — VND is zero-decimal, so `amount` is the plain số tiền, never ×100; VND on a US test account is confirmed working). Both the intent (`payment_method_types:["card"]`) and the Element (`paymentMethodTypes:["card"]`) are **pinned to card on purpose**: `automatic_payment_methods` would offer redirect-based methods, and a redirect after payment would leave the SPA — losing the wizard state and the seat hold, i.e. money taken with no ticket. It also makes Stripe demand a `confirmParams.return_url`, which card-only avoids entirely. The PaymentIntent is created only when the user presses "Thanh toán" (so abandoning the step leaves no orphan intents), and `confirmPayment({redirect:"if_required"})` keeps the user **on the page**, which is what preserves the wizard state, the seat hold and the 8-minute timer. The single action button stays `OrderSummary`'s `.os-k__cta`; `PaymentStep` registers its submit function on a ref the wizard holds (and those handlers are deliberately **not** memoised — memoising would freeze an old `selected`). After `succeeded`, the client POSTs the booking with `paymentRef: pi_…`; the gateway retrieves that intent from Stripe, re-computes the amount from the DB, and **overwrites `seatTotal`/`fnbTotal`/`serviceFee`/`totalPrice` with its own numbers** before inserting — so the client cannot decide what it pays. `Booking.paymentRef` is `@unique`: re-sending the same reference returns the **existing** booking (200) instead of creating a duplicate, which is what makes a retry after a dropped connection safe. Non-card requests get `paymentRef` stripped from the body.

**After a booking is created the gateway sends the ticket by email** — `void sendTicketEmail(id, pickLang(req.headers["x-lang"]))`, fired *after* the response is already out, so nothing about it can delay or fail the booking; the client picks the language by sending `x-lang` from `i18n.language` on `createBooking`. The idempotent replay branch (same `paymentRef` re-sent after a dropped connection) deliberately does **not** send a second email. Step ④ and every ticket on "Vé của tôi" carry a `<ResendTicketButton>`, which renders `null` unless `useEmailConfig()` says email is configured.

## Testing

- **Vitest — 503 unit tests in two projects** (configured in `vite.config.mjs`): **`client`** (happy-dom, `src/**/*.test.ts(x)`, setup `src/test/setup.ts`) and **`server`** (node, `server/**/*.test.ts`, setup `server/src/test/setup.ts`). Everything from pure helpers up to whole pages is covered; the only deliberate blanks are the Prisma singleton (mocked), `src/styles/fonts.ts`, and `App.tsx`/`index.tsx` (entry points — e2e covers those). The client setup **imports `i18n`** so `useTranslation()` resolves to the **vi** default (tests assert Vietnamese strings; `MovieCard` shows "Khoa học viễn tưởng", not the DB genre code).
- **Nothing is mocked at the module level.** Client tests go through the real `services/*` + `queries/*` and are answered by **MSW** (`src/test/msw/`), with `onUnhandledRequest:"error"` so a forgotten endpoint fails the test instead of hanging. Writable tables live in the exported **`db` object** (`movies`/`rooms`/`showtimes`/`bookings`/`reviews`) which handlers read **at call time** and `resetFixtureDb()` restores after each test — that is why a mutation + `invalidateQueries` really changes the list a test then asserts on. Handlers must mirror the real server's shape (`occupied-seats` → `{showtimeId, seats}`, logout/hold-release → **204**, POST → 201): a handler that "looks right" but differs is how you get green tests over broken code. Server tests fire **supertest** at the real Express app with a mocked Prisma (`server/src/test/prismaMock.ts`) and a session cookie named **`at`** built by `signAccess(id, role)`, so they run in CI without a database.
- **`renderWithProviders`** (`src/test/renderWithProviders.tsx`) wraps a component in QueryClient + MemoryRouter + AuthProvider. Its **`waitForAuth` gate defaults to true** and mirrors `AppShell`: children don't render until the session check resolves, so **`await findBy…` before asserting** (a `getBy…` straight after render sees an empty DOM). Pass `user` to be logged in, `route`/`state` for the location.
- **Coverage is a CI gate.** Run **`npm run test:cov`** — `npm run test:run` does *not* enable coverage, so thresholds are silently skipped there (and no table is printed, which reads exactly like "the numbers didn't change"). Thresholds live in `vite.config.mjs` (statements/lines 90, branches 87, functions 84) and sit ~1 point under the measured 90.93/88.37/85.56. Per-area numbers and the slice-by-slice history are in `docs/superpowers/plans/2026-07-29-testing-coverage-baseline.md`.
- **Test-writing traps already paid for** (all cost a red test that looked like an app bug): fake timers must be installed **before render** — install them after and the first `setTimeout` was already scheduled on the real clock, so the chain breaks and e.g. `SeatHoldTimer` never counts down; prefer `vi.useFakeTimers({shouldAdvanceTime:true})` + `fireEvent` (userEvent with fake timers has hung the suite twice). recharts draws nothing under happy-dom because `ResponsiveContainer` measures 0px — mock it into a fixed-size box (see `AdminOverview.test.tsx`) or the chart data goes unasserted. Admin modals use bare `<label>` without `htmlFor` (walk up to `.adm-k__field` instead of `getByLabelText`), and their confirm buttons repeat the row action's name — scope every in-dialog query with `within(getByRole("dialog"))`. `input[type=date|time]` needs `fireEvent.change`, not typing. `KineticHeading` splits text per word/char, so match on a container's `textContent`. Read the error path through **`HttpResponse.error()`**, not a 500 — see the `r.ok` note under Conventions.
- **Playwright** in `e2e/` — four files with different rules. `smoke.spec.ts` (20 tests) is **read-only**: it logs in as the seeded admin and asserts core flows render (including the MovieDetail reviews section, the global search dropdown, the `/search` page, Movies' URL-synced filters, an **EN language toggle** that flips nav labels + `<html lang>`, and the **PWA head links** — `apple-touch-icon` + `theme-color`, which are static in `index.html` so they exist even in dev where the SW is off), and must never create data. `booking.spec.ts` (1 test) is the **full booking flow that really writes**: normal user → seat → F&B → payment → e-ticket → "Vé của tôi", then **deletes its own booking via the admin API in a `finally`**. `reviews.spec.ts` (1 test) **writes too**: normal user posts a review on a movie they haven't reviewed in the seed (movie 7, to avoid the `@@unique` 409), asserts it appears, deletes it via the UI, and a `finally` sweeps any leftover test review via the admin API — so both the CI database and the local Neon dev branch stay clean. `payment.spec.ts` (1 test) drives the **real Stripe card flow** (finds the card iframe by walking `page.frames()` — Stripe nests several iframes sharing one title and builds them progressively, so a plain `page.locator('iframe[title=…]')` snapshot misses it — fills `input[name="number"|"expiry"|"cvc"]` rather than placeholders, which change with locale, asserts the e-ticket shows `pi_…`, deletes the booking in a `finally`) but calls `test.skip` when `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` are absent — so it runs on a dev machine that has keys and **self-skips in CI**, which keeps CI off Stripe's network entirely (no GitHub secret needed). `playwright.config.ts` imports `dotenv/config` purely so that skip check can see `.env`. The write tests pick the **last** cinema/showtime/seat (or a not-yet-reviewed movie) so they can't collide with the smoke test while Playwright runs them in parallel. Because the card method becomes the default once keys exist, `booking.spec.ts` explicitly clicks "Thanh toán tại quầy" before paying. The login placeholders `your@email.com` / `••••••••` and the button text **"Đăng nhập"** are load-bearing for all four — don't change them. `smoke.spec.ts`'s Vietnamese-glyph guard **must `await document.fonts.load()` before measuring**: `@fontsource` splits each family into `unicode-range` faces and the browser only fetches the subset the screen actually needs, so a synchronous measurement is really testing "did this page happen to render Vietnamese text in that family" — it went red the moment section headings moved off Anton, with the fonts perfectly fine.
- **`e2e/a11y.spec.ts` is the accessibility gate** — `@axe-core/playwright` scans **16 states**: 8 public pages, `/tickets`, all three first steps of the booking wizard, four admin pages, **and one open modal** (dialogs are where a11y bugs hide, and scanning a closed page never sees them). It runs **inside the existing `e2e` job**, so the gate count stays six — a11y is not a seventh gate. It **fails on `critical` + `serious`** only; `moderate`/`minor` are logged, because a gate that flags arguable violations on day one is a gate someone eventually switches off. `EXCLUDED_RULES` is **empty** and anything added needs a written reason. It sets **`test.use({ reducedMotion: "reduce" })`** so axe measures final colours: the hero fades in over 480ms and a loaded CI machine let axe sample it at ~86% opacity, reporting a 4.33:1 contrast failure that does not exist. It runs axe in a **real browser with real CSS**, which is the whole point: happy-dom applies no CSS, so unit tests are blind to contrast — the class that made up 23 of the 26 violations found. Like the smoke test it is **read-only**: it walks to step ③ and never pays. **When a scan target sits inside `<Reveal>`, scroll to it and wait for `.is-visible` first** — otherwise the element is still `opacity:0`, axe skips it, and the check silently passes on luck (this is exactly how a contrast bug reached CI green locally and red on CI).
- **The ticket email has no e2e on purpose.** Playwright can't read an inbox, and clicking "gửi lại" for real would burn provider quota sending to seeded fake addresses. Its guarantees come from the pure unit tests (`server/src/email/templates.test.ts` + `lang.test.ts`) plus `booking.spec.ts`, which books for real: with a key present the background send fires at `a@cinema.vn`, Resend rejects it, and the spec still has to go green — that is the regression test for "a mail failure must never break a booking".

## Conventions

- The UI is **bilingual (vi/en) via `react-i18next`** — **all user-facing copy must go through `t("area.key")`**, never a hardcoded string (aria-labels included). See the i18n section below; when adding copy, add the key to **both** `src/i18n/locales/vi.json` and `en.json`.
- Prices are **always VND** regardless of language — format with **`formatPrice(n)`** from `i18n/format` (`n.toLocaleString(locale) + " ₫"`), not a raw `.toLocaleString("vi-VN")`. Dates/times are locale-aware via **`formatDateTime`/`formatDate`** from the same module, or one of its presets `formatDayShort`/`formatShowtimeLabel` (exception: 24-hour clock-only labels go through **`formatClock`**, which deliberately stays `en-GB`).
- Keep the six CI gates green; lint must have **0 warnings** (a warning fails CI). Handle a legitimate `react-hooks/exhaustive-deps` or `react-refresh` case with an annotated `// eslint-disable-next-line`, not by loosening rules.
- **The read path in `src/services/api.ts` deliberately does not check `r.ok`** — a non-2xx body is passed through as if it were data, so `isError` only fires on a real network failure. This was reviewed and **kept** (2026-08-01); it is not an oversight to "fix" in passing. Tests that need the error branch must use `HttpResponse.error()`. The write path (`createBooking`, review POST/PATCH, admin CRUD) does throw on non-ok.
- **There are three reds, and they must not be merged.** `--red` (#e63030) is the brand mark — borders, hard shadows, the huge display numbers (large text only needs 3:1, so it passes). `--red-bg` (#dd2b2b) is the only red allowed **behind white text**; `--red-text` (#ff5a5a) is the only red allowed **as small text on a dark ground**. #e63030 fails both of those at 4.34:1, and the two roles pull in opposite directions — one needs a darker red, the other a lighter one. Merging them back re-breaks the a11y gate. Likewise `--text-dim`/`--text-muted` were raised to #8a8a8a/#a3a3a3; the old #555 measured 2.65:1.
- **Never dim text with `opacity` to signal a secondary state** — it silently destroys contrast, and it did so three separate times here (the booking stepper, its step numbers, and the showtime button meta). Express the state with a color token instead.
- Query lists used inside a downstream `useMemo` should be stabilized: `const xQ = useHook(); const x = useMemo(() => xQ.data ?? [], [xQ.data])` (a bare `?? []` creates a new array each render and trips exhaustive-deps).
